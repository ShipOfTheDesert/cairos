(library
 (name finance_test_helpers)
 (modules finance_test_helpers)
 (libraries cairos cairos_finance nx alcotest))

; (executable) not (test): this binary serves double duty — self-contained
; infra tests now, fixture-dependent cross-validation later. Fixtures are
; generated by Python (just validate-generate), so dune runtest must not
; invoke it. Runs via just validate-check / just validate instead.

(executable
 (name cross_validate)
 (modules cross_validate)
 (libraries finance_test_helpers cairos cairos_finance alcotest))

(test
 (name test_metrics)
 (modules test_metrics)
 (libraries finance_test_helpers cairos cairos_finance nx alcotest))
